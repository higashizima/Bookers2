
<p>投稿ユーザー画像：<%= image_tag @user.get_profile_image(100,100) %></p>
<p>なまえ：<%= @user.name %></p>

<%= render 'books/list' %>


<% @users.each do |user| %>
  <div>
    <p>名前：<%= user.name %></p>
    <%= link_to user_path(user.id) do %>show<% end %>
  </div>
<% end %>